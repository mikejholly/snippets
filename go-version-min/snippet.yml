version: 0

name: Require min Go version
description: Test a previously captured Go version

action:
  language: node
  main: index.js

inputs:
  min-version:
    description:
      The minimum required version
